name = "content-studio-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# Secrets (do NOT put keys here — set them with `wrangler secret put`):
#   wrangler secret put OPENAI_API_KEY
#   wrangler secret put ALLOW_ORIGIN     # recommended, e.g. https://your-app.pages.dev
#   wrangler secret put ACCESS_TOKEN     # recommended, a shared team password
